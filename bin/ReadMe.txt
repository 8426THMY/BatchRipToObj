To use this program, create a batch file in the
same directory as it with the following contents:


BatchRipToObj.exe -i "inputDir" -o "outputDir"


"inputDir" is the directory containing the files you
want to convert, and "outputDir" is where to put the
converted files.